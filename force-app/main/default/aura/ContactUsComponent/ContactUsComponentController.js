({
	doInit : function(component, event, helper) {
	/*	var prdList = component.get("c.getProductInfo");
       
        
        prdList.setCallback(this, function(data) 
                           {
                               component.set("v.ProductList", data.getReturnValue());
                           });
        $A.enqueueAction(prdList);
        */
        
 },})