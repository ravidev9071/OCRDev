({
    handleNext: function(component,event,helper){
        
        helper.handleNextButton(component,event);  
        
    },
    handlePrescriberNext: function(component,event,helper){
        
        helper.handlePrescriberNextButton(component,event);
        
    },

    handlePrevious: function(component,event,helper){
        
        helper.handlePreviousButton(component,event);  
        
    },
})